//
//  ContentView.swift
//  Event Countdown
//
//  Created by Spiros Raptis on 12/29/23.
//

import SwiftUI

import SwiftUI

// Assuming Event struct and EventRow views are defined in their own files or earlier in this file
// Event struct code goes here if not in a separate file...

struct ContentView: View {
    var body: some View {
        EventsView()
    }
}

// EventsView code goes here if not in a separate file...
// EventRow view code goes here if not in a separate file...

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
